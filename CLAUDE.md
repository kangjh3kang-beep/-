# CLAUDE.md - AI Assistant Guide

This file provides guidance for AI assistants (like Claude) working with this repository.

## Repository Overview

**Repository Name:** `-`
**Owner:** kangjh3kang-beep
**Status:** Newly initialized project

This repository is in its early stages with minimal initial structure.

## Current Project Structure

```
/
├── .git/           # Git version control
├── README.md       # Project readme
└── CLAUDE.md       # This file - AI assistant guide
```

## Development Workflow

### Git Conventions

- **Main Branch:** `main` (default)
- **Commit Messages:** Use clear, descriptive commit messages
  - Format: `<type>: <description>`
  - Types: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`
  - Example: `feat: add user authentication module`

### Branch Naming

- Feature branches: `feature/<description>`
- Bug fixes: `fix/<description>`
- Documentation: `docs/<description>`

## Commands Reference

Since this is a new repository, commands will be added as the project develops. Common patterns to follow:

```bash
# Check project status
git status

# View recent history
git log --oneline -10
```

## Code Style Guidelines

*(To be defined as the project develops)*

When code is added, follow these general principles:
- Write clean, readable code with meaningful names
- Include comments for complex logic
- Follow language-specific conventions and best practices

## Testing

*(To be defined as the project develops)*

When tests are added:
- Run all tests before committing
- Maintain test coverage for new features

## AI Assistant Guidelines

### When Working on This Repository

1. **Read before modifying:** Always read existing files before making changes
2. **Preserve structure:** Maintain existing code organization patterns
3. **Document changes:** Update this CLAUDE.md when adding significant new components
4. **Commit atomically:** Make focused commits with clear messages
5. **Don't over-engineer:** Keep solutions simple and focused on the task

### Things to Avoid

- Don't add unnecessary dependencies
- Don't create files without clear purpose
- Don't modify configuration without explicit request
- Don't guess at undocumented conventions

### Updating This File

Update CLAUDE.md when:
- Adding new directories or major components
- Establishing new development patterns
- Adding build/test/deployment workflows
- Introducing new dependencies or tools

## Notes

- This repository uses Git for version control
- The project is in early development; structure will evolve
- Check README.md for project-specific documentation
